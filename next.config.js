/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aktifkan jika Anda menggunakan middleware API route
  // experimental: {
  //   middleware: true,
  // },
};

module.exports = nextConfig;
